// 7 Primitive Types
// boolean, number, string, Symbol, undefined, null, BigInt

let num = 3n + 4n;

// Object

{
  let location: string;
  let name = "Kevin";
  let age: number | string = "42";

  location = "Belfast";
}
{
  // any, void, unknown, never
}
